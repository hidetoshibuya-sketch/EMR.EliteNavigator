# audio/ — generated MP3 files

Run `./generate_audio.sh` from the pwa/ folder on your Mac to populate this
directory with ~530 pre-recorded prompts (female native voice, Samantha).

Until then, the app gracefully falls back to live TTS (Web Speech API).
